{{#if (hasContextNotes @root.actor target)}}
<span class="span3"><br/>{{localize "PF1.ContextNotes"}}</span>
{{#each (contextNotes @root.actor target)}}
  <span class="tooltipcontent-context">{{{this}}}</span>
{{/each}}
{{/if}}

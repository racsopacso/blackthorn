<form autocomplete="off" onsubmit="event.preventDefault();">
  <div class="selectors">
    {{!-- PC selector --}}
    <h2>{{localize "PF1.PCs"}}</h2>
    <div class="character-selector">
      {{#each actors.characters as |actor|}}
        <div class="actor{{#if actor.toggled}} toggled{{/if}}" data-id="{{actor.id}}">
          <img class="portrait" src="{{actor.actor.img}}">
          <span class="name">{{actor.actor.name}}</span>
          <input type="checkbox" {{checked actor.toggled}}/>
        </div>
      {{/each}}
    </div>

    {{!-- NPC selector --}}
    <h2>{{localize "PF1.NPCs"}}</h2>
    <div class="npc-selector">
      {{#each actors.npcs as |actor|}}
        <div class="actor{{#if actor.toggled}} toggled{{/if}}" data-id="{{actor.id}}">
          <img class="portrait" src="{{actor.actor.img}}">
          <span class="name">{{actor.actor.name}}</span>
          <span class="xp">{{actor.xpString}} XP</span>
          <input type="checkbox" {{checked actor.toggled}}/>
        </div>
      {{/each}}
    </div>
  </div>

  <div class="footer">
    {{!-- Options --}}
    <h2>{{localize "PF1.Options"}}</h2>
    <div class="options flexcol">
      <div class="bonus-xp form-group">
        <label>{{localize "PF1.Application.XPDistributor.Options.BonusExperience"}}
          <input class="form-fields" type="number" value="{{bonusXP}}" />
        </label>
      </div>
    </div>

    {{!-- Show result --}}
    <div class="flexcol result">
      {{!-- Split XP --}}
      <div class="property xp-split">
        <strong>
          <i class="fas fa-people-arrows"></i> {{localize "PF1.Application.XPDistributor.SplitExperience"}}
        </strong>
        <strong class="value">{{labels.xp.split}}</strong>
      </div>

      {{!-- Total XP --}}
      <div class="property xp-total">
        <strong>
          <i class="fas fa-star"></i> {{localize "PF1.Application.XPDistributor.TotalExperience"}}
        </strong>
        <strong class="value">{{labels.xp.total}}</strong>
      </div>
    </div>

    {{!-- Buttons --}}
    <div class="flexrow">
      <button type="button" name="split-evenly">
        <i class="fas fa-people-arrows"></i> {{localize "PF1.Application.XPDistributor.SplitEvenly"}}
      </button>

      <button type="button" name="give-to-all">
        <i class="fas fa-star"></i> {{localize "PF1.Application.XPDistributor.GiveToAll"}}
      </button>

      <button type="button" name="cancel">
        {{localize "PF1.Cancel"}}
      </button>
    </div>
  </div>
</form>
